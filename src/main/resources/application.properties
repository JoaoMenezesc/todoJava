spring.datasource.url=jdbc:postgresql://localhost:5432/todo_db
spring.datasource.username=jovmd
spring.datasource.password=199169!

# Define o driver correto para PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuração do Hibernate para atualizar automaticamente o schema
spring.jpa.hibernate.ddl-auto=update

# Exibir SQL gerado pelo Hibernate no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialeto do Hibernate para PostgreSQL (opcional, mas recomendado)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
